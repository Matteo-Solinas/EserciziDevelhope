spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: root
    password: 1234
    driver-class-name: org.h2.Driver
  jpa:
    database: h2
    show_sql: true
    hibernate:
      ddl-auto: update
    jackson:
      serialization:
        fail-on-empty-beans: false
